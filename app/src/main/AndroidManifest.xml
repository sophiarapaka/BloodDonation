<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blooddonation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MyApp">

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity" android:exported="true" />
        <activity android:name=".ForgotPasswordActivity" android:exported="false" />
        <activity android:name=".CreateAccountActivity" android:exported="false" />
        <activity android:name=".DonationHistoryActivity" android:exported="true"/>
        <activity android:name=".EligibilityCheckActivity" android:exported="true"/>
        <activity android:name=".FindCentersActivity" android:exported="true"/>
        <activity android:name=".DonationTipsActivity" android:exported="true"/>


    </application>

</manifest>
